import { PopulationRounderPipe } from './population-rounder.pipe';

describe('PopulationRounderPipe', () => {
  it('create an instance', () => {
    const pipe = new PopulationRounderPipe();
    expect(pipe).toBeTruthy();
  });
});
